logs/
.venv/
venv/
dist/
build/
# editable install metadata (pip install -e . -> v_dance.egg-info/)
*.egg-info/
.idea/
**/actual_states/*
**/__pycache__/*
creds
*.xlsx
*.txt
suspect_decisions.py
teams/teams
data/smogon_stats_cache
data/pkmn_sets_cache
data/local_data
data/vods
# VGC-Bench redistributable raw logs (601MB; the 525MB regmabo3 JSON exceeds GitHub's 100MB
# per-file limit — this is the actual push blocker). Fetch from HF cameronangliss/vgc-battle-logs.
data/hf_vgc_bench/
# trained model checkpoints — binary weights (~184MB of .pt, mostly backups + retired experiments);
# distribute the deployed one via a GitHub Release, not source control.
ai_train_scripts/
.rust/
.env
target/
*.pyc
models/*.pt
models/*.pth
# all runtime outputs live under artifacts/ (self_play_archive, replay_buffer, logs, eval_results)
artifacts/
# defensive: poke-env's NATIVE replay dump writes ./replays/ relative to CWD. We disable it
# (we save categorized .html under artifacts/.../live/<stamp>/gen_N/{replays,eval}); this guards
# against any stray dump ever being committed.
/replays/
v_dance/selfplay/replays/
# Pokémon Showdown local server (cloned by setup.sh — not part of this repo)
pokemon-showdown/

# agent memory index archive (not project source)
memory_before.md

# ── kept local, not published to the public branch ───────────────────────────
# IDE / agent config
.vscode/
.claude/
# dev harnesses, workflow scripts, throwaway artifacts
scratch/
# personal run-configs (kept local; config.example.json stays tracked as the template/schema reference)
config/
# fork leftovers staged for deletion
Delete_When_Project_Done/
# tests (run locally; not published)
tests/
data/scripts/tests/
.pytest_cache/
# design docs / notes
docs/
# all markdown except the root README.md
*.md
!/README.md
